export class EntityManager {
    static entities = {};

    static create = (type) => {
        switch(type) {
            case 'rectangle':
                
                break;
        }
    };
};